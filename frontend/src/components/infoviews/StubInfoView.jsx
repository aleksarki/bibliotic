import "./InfoView.scss";

function StubInfoView() {
    return (
        <div className="InfoView">
            <div className="view-title">Выберите элемент.</div>
        </div>
    );
}

export default StubInfoView;
